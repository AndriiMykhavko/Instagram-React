import React from 'react'
import './addPostButton.scss'

const AddPostButton: React.FC = (props: any) => {
  return(
      <button className="addPostButton">Add Post</button>
  )
}

export default AddPostButton;